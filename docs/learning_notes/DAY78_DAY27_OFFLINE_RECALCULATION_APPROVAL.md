# Day 78：Day 27 离线重算审批

Day 78 不重算光学指标，也不连接 ZOS-API。它只审批下一阶段使用现有实测证据重新计算定位误差包络。

旧 Day 27 使用 `±0.010 mm` 教学误差。恢复链使用 `±0.012 mm`，因此四个命令中心需要引用不同的精确端点。原有 16 点加上 Day 76 新测的 7 点，共形成 23 点证据池。

重算仍必须遵守：只使用精确实测点；四指标 AND；禁止插值、外推和曲线拟合；三个采样状态通过不代表连续区间通过；不产生唯一工程赢家。

Day 78 的审批不释放 Slot 6。重算完成后还需要单独 CP09 科学复核。
