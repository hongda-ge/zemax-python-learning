# Day 79：Day 27 离线包络重算

Day 79 消费 Day 78 的一次性审批，把 Day 25 的 16 个实测点与 Day 76 的 7 个恢复点合并为 23 点证据池。

四个命令中心分别使用 `-0.012 / 0 / +0.012 mm` 三个相对状态。每个状态必须是精确实测点，并按冻结的 Spot mean、Spot worst、MTF30 minimum、MTF50 minimum 四指标 AND 规则重新判断。

本步骤不连接 ZOS-API，不产生新光学指标，不插值、不外推、不拟合。通过三个采样状态仍不能证明连续区间通过，也不能产生唯一工程赢家。
